{% form_theme form 'bootstrap_5_layout.html.twig' %}


			{{ form_start(form) }}			
			{{ form_row(form.status, {'label' : 'Etat de la commande : '}) }}
			<button class="btn btn-primary">{{ button_label|default('valider') }}</button>
			{{ form_end(form) }}

