{% extends 'base.html.twig' %}

{% block title %}TVA{% endblock %}

{% block body %}
    <h1>TVA</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ t_v_a.id }}</td>
            </tr>
            <tr>
                <th>Rate</th>
                <td>{{ t_v_a.rate }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tva_index') }}">back to list</a>

    <a href="{{ path('tva_edit', {'id': t_v_a.id}) }}">edit</a>

    {{ include('tva/_delete_form.html.twig') }}
{% endblock %}
