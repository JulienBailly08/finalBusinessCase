<form method="post" action="{{ path('quantity_on_command_delete', {'id': quantity_on_command.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quantity_on_command.id) }}">
    <button class="btn">Delete</button>
</form>
